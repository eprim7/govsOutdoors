import Header from "../../components/Header/Header";

function Schedule(){
    return(
        <>
        <Header />
        <h1>I figured we could have like a grid or something idk to like let everyone know when the trips are</h1>
        </>
    );
}

export default Schedule;